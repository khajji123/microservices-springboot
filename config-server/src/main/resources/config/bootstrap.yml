spring:
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      server:
        git:
          uri: https://github.com/jerbi2026/Config-repo.git
          clone-on-start: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
    registerWithEureka: true

